<div class="navbar">
  <h1 class="title"><a href="/" class="nav-root-link">Enginestarter</a></h1>
  <% if current_user %>
    <%= button_to "Sign Out", session_url, method: :delete, class: 'sign-out' %>
  <% else %>
    <%= link_to "Sign Up", new_user_url %>
    <%= link_to "Log In", new_session_url %>
  <% end %>
</div>
